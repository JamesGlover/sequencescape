default: &default
  adapter: postgresql
  encoding: unicode
  database: sequencescape_pg_development
  username: inbox_app
  password:
  pool: 5
  timeout: 5000

development:
  <<: *default
  database: sequencescape_pg_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: sequencescape_pg_test

production:
  <<: *default
  database: sequencescape_pg_
